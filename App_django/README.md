# Django REST Framework CRUD

### Requerimientos

* Python 3.8

### Instalación

Considerar estos comandos para la contenerización e instalación

```
python -m venv venv
./venv/Scripts/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```
